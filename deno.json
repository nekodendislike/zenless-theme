{
	"tasks": {
		"build": "deno task reload && deno run -A --unstable-unsafe-proto npm:vsce package",
		"reload": "deno run --allow-write --allow-read ./builder/gen.ts",
		"package": "vsce package",
		"publish": "deno run -A --unstable-unsafe-proto npm:vsce publish"
	}
}